# Feature Specifications

This directory will contain feature specifications created using the `/speckit.specify` command.

Each feature will be in its own subdirectory with the following structure:
- `spec.md` - Feature specification
- `plan.md` - Implementation plan
- `tasks.md` - Task list
- `data-model.md` - Data models (if needed)
- `contracts/` - API contracts (if needed)

